{
  "name": "totalmix-mcp-server",
  "version": "0.7.3",
  "private": true,
  "description": "MCP server for RME TotalMix FX Global OSC. Long-running HTTP daemon with a shared OSC state cache.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "bundle": "esbuild src/stdio.ts --bundle --platform=node --format=esm --external:serialport --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" --outfile=../plugins/totalmix/server/totalmix-mcp-stdio.mjs",
    "build": "tsc",
    "start": "node --env-file=.env dist/index.js",
    "dev": "tsc && node --env-file=.env dist/index.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "start:stdio": "node dist/stdio.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "express": "^4.21.2",
    "osc": "^2.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.7.2"
  }
}
